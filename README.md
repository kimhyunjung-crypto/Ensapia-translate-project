# ENSAPIA 번역 워크벤치

ENSAPIA Seoul의 한국어↔일본어 Slack 비즈니스 메시지 번역을 위한 로컬 전용 Next.js 앱입니다.

## 준비

- Node.js 20.9 이상
- npm

```powershell
npm install
npm run db:init
```

`db:init`은 `.env.local`의 32바이트 암호화 키를 값 노출 없이 만들고, SQLite 마이그레이션과 가짜 연습용 데이터를 준비합니다. 생성된 `DATA_ENCRYPTION_KEY`를 잃으면 기존 암호화 데이터를 복구할 수 없으므로 `.env.local`은 안전한 장소에 별도로 보관하세요.

AI 기능을 연결할 때 `.env.local`에 `OPENAI_API_KEY`, `GEMINI_API_KEY`를 입력하세요. 실제 회사 메시지를 사용하기 전까지 `DATA_MODE=demo`를 유지합니다. 비밀 값은 브라우저로 전달하거나 화면에 표시하지 않습니다.

## 실행

```powershell
npm run dev
```

브라우저에서 `http://127.0.0.1:3000`을 엽니다. 개발·운영 서버 모두 `127.0.0.1`에만 바인딩됩니다.

현재 번역 화면은 한국어·일본어 입력, 3,000자 검사, 방향 자동 감지, 진행·결과·복사·재시도 흐름을 제공합니다. 서버는 암호화된 활성 인명·용어·말투 규칙을 찾아 인명·호칭 → 회사 용어 → 상황별 말투 순으로 고정하고, 변경하지 않은 원문과 같은 규칙 목록으로 OpenAI·Gemini 1차 번역과 교차검토를 병렬 실행한 뒤 OpenAI 최종 종합과 품질검사를 거칩니다. 중간 결과는 화면에 표시하지 않고 암호화해 저장합니다.

각 AI 단계는 성공·실패·처리 시간·시도 횟수를 기록하며 실패한 단계만 한 번 자동 재시도합니다. 원문과 후보 결과는 항상 신뢰할 수 없는 번역 데이터 경계 안에 넣어 원문 속 명령을 실행하지 않습니다. 번역 완료 후 기록 저장만 실패하면 별도 안내와 `저장 다시 시도` 버튼이 나타나며, 완료된 결과를 메모리에서도 암호화해 잠시 보관하므로 AI를 다시 호출하지 않고 저장만 복구합니다.

용어집 화면에서는 원어·권장 표기·설명·금지 표기와 사용 상태를 검색·필터링하고 등록·수정할 수 있습니다. 번역에 사용되지 않은 용어만 완전히 삭제할 수 있으며, 사용 이력이 있는 용어는 중지·재활성화합니다. 화면의 `예시 파일`을 내려받아 같은 CSV 열 순서(`sourceText,targetText,direction,description,forbiddenTerms,isActive`)로 최대 500개를 가져올 수 있고 실패 행은 행 번호와 이유를 표시합니다. 모든 용어 내용과 상세 변경 기록은 암호화해 저장하며 활성 용어만 다음 번역부터 적용합니다.

인명·호칭 화면에서는 여러 한국어 인식 표현을 Enter 또는 쉼표로 태그처럼 등록하고, 하나의 일본어 통합 표기와 역방향 한국어 통합 표기를 함께 관리합니다. 저장 전 양방향 변환을 바로 미리 볼 수 있으며, 정규화된 인식 표현의 전역 중복을 차단합니다. 활성 규칙만 다음 번역부터 적용되고, 사용 이력이 없는 규칙만 완전히 삭제할 수 있습니다.

운영 설정의 말투 규칙에서는 인사·요청·거절·사과·독촉·확인·감사 기본 상황과 사용자 정의 상황을 관리합니다. 권장 어조, 쿠션어, 금지 표현, 예문을 등록·수정하고 버전별 이전·변경 값을 확인할 수 있습니다. 활성 규칙만 AI 번역 조건에 포함되며, 번역에 사용된 규칙은 삭제 대신 중지·재활성화할 수 있습니다. 규칙 내용과 상세 변경 기록은 로컬 데이터베이스에 암호화해 저장합니다.

활성화된 `첫인사`는 일반 말투와 분리된 최우선 필수 규칙입니다. 메시지 첫머리의 `안녕하세요.`, `수고하십니다.`, `수고 많으십니다.` 계열은 일본어 `お疲れ様です。`로, 첫머리의 `お疲れ様です` 계열은 한국어 `안녕하세요.`로 강제합니다. 두 1차 번역·두 교차검토·최종 종합 프롬프트에 같은 Hard Rule을 전달하며, 최종 출력 직전에도 코드가 시작 표기를 검사하고 자동 교정합니다. 문장 중간의 같은 표현에는 적용하지 않습니다.

운영 설정의 AI·비용·저장 영역에서는 OpenAI·Gemini 연결과 현재 `demo/real` 모드, 5단계 모델·지시문 버전을 확인하고 모델 ID를 새 버전으로 교체할 수 있습니다. 최근 한 건·오늘·이번 달의 호출, 토큰과 예상 비용을 KST 기준으로 집계하며 기본 월 한도 $10의 80%부터 경고하고 한도 도달 뒤에는 번역마다 실행을 확인합니다. 단가는 [OpenAI GPT-5.6 Luna 공식 문서](https://developers.openai.com/api/docs/models/gpt-5.6-luna)와 [Gemini 공식 가격표](https://ai.google.dev/gemini-api/docs/pricing)의 적용일별 값을 사용합니다. 기간별 삭제는 원문과 모든 번역 결과를 영구 삭제하지만 원문 없는 사용량 합계와 삭제 사실은 유지합니다.

운영 설정 하단의 `전체 품질과 완료 점검`에서는 번역·용어·인명·말투·비용·삭제 통합 점검, 한국어→일본어 25건과 일본어→한국어 25건의 데모 회귀 평가표, 핵심 색 대비 결과와 PRD 대조 체크리스트를 확인할 수 있습니다. 50건 평가는 가짜 메시지의 고정된 회귀 기준선이며 실제 API 번역을 업무에 승인하기 전에는 별도의 사람 검수가 필요합니다. 처리 시간 감소율과 최종본 수정 횟수는 실제 사용 전후에 측정하는 운영 검증 항목으로 구분해 표시합니다.

기본 `DATA_MODE=demo`에서는 외부 API 비용 없이 같은 5단계 흐름을 연습용 응답으로 점검합니다. 승인된 실제 메시지와 유료 API 계정을 사용할 때만 `DATA_MODE=real`로 바꾸면 OpenAI Responses API와 Gemini Interactions API 어댑터가 실행됩니다.

데이터 연결과 14개 표의 저장 건수는 다음 명령 또는 `/settings` 화면에서 확인할 수 있습니다.

```powershell
npm run db:status
```

## 검증

```powershell
npm run lint
npm test
npm run build
npm run test:e2e
```

`npm run test:e2e`는 앱 서버를 자동으로 실행하며 Chromium 브라우저가 필요합니다. 최초 한 번은 `npx playwright install chromium`으로 준비할 수 있습니다.
